# ============================================================
#  requirements.txt — paste into Kaggle notebook cell first
#  or pip install -r requirements.txt locally
# ============================================================

torch>=2.0.0
torch-geometric>=2.3.0
torch-scatter
torch-sparse
flwr>=1.5.0
imbalanced-learn>=0.11.0
scikit-learn>=1.3.0
pandas>=2.0.0
numpy>=1.24.0,<2.0.0
pyngrok>=6.0.0       # Phase 4 only (real deployment)
matplotlib>=3.7.0
seaborn>=0.12.0


# ============================================================
#  QUICK START — paste these cells into your Kaggle notebook
# ============================================================

# CELL 1 — Install dependencies
# """
# !pip install torch-geometric flwr imbalanced-learn pyngrok -q
# !pip install torch-scatter torch-sparse -f \
#     https://data.pyg.org/whl/torch-2.0.0+cu117.html -q
# """

# CELL 2 — Run Phase 1
# """
# %run phase1_preprocessing.py
# """

# CELL 3 — Enable GPU then run Phase 2
# (Kaggle: Settings → Accelerator → GPU T4 x1)
# """
# %run phase2_model.py
# """

# CELL 4 — Run Phase 3 (FL simulation)
# """
# %run phase3_federated.py
# """

# ============================================================
#  EXPECTED RESULTS (approximate, will vary)
# ============================================================
#
#  Phase 2 — Centralised STGNN (single machine):
#    AUC-PR:    ~0.72 – 0.81
#    AUC-ROC:   ~0.88 – 0.93
#    F2-Score:  ~0.65 – 0.74
#    Recall:    ~0.78 – 0.86
#    Precision: ~0.52 – 0.64
#
#  Phase 3 — Federated (3 banks, 20 rounds):
#    AUC-PR:   ~0.68 – 0.78  (slight drop from non-IID — expected)
#    F2-Score: ~0.61 – 0.71
#    Recall:   ~0.74 – 0.83
#
#  The ~4-6% drop from centralised → federated is the
#  "privacy cost" — the core finding of your paper.
#  This matches the literature survey findings (Sun 2025,
#  FED-SPFD 2025).
#
# ============================================================
#  FILE STRUCTURE
# ============================================================
#
#  your_project/
#  ├── phase1_preprocessing.py   ← run first
#  ├── phase2_model.py           ← run second (needs GPU)
#  ├── phase3_federated.py       ← run third
#  ├── phase4_ngrok.py           ← real deployment (optional)
#  ├── requirements.txt          ← this file
#  └── artifacts/                ← auto-created
#      ├── preprocessed.pkl      ← output of Phase 1
#      ├── best_model.pt         ← output of Phase 2
#      ├── centralised_metrics.pkl
#      └── fl_history.pkl        ← output of Phase 3
